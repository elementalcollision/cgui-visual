/// <reference types="vite/client" />

// Defined by Vite at build time (see vite.config.ts `define`).
// Single source of truth: app/package.json's "version" field.
declare const __APP_VERSION__: string;
